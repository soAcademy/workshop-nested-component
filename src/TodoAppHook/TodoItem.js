const TodoItem = ({
    handleToggleTask,
    item
}) => {
    const handleClick = (e) => {
        { /* To do: use handleToggleTask function to update the state */ }
    }

    return  (
        { /* To do: render to do */ }
    )
}

export default TodoItem