import TodoItem from "./TodoItem"

const TodoList = ({
    handleToggleTask,
    items
}) => (
    <ul>
        { /* To do: map params and render to do items here */ }
    </ul>
)

export default TodoList